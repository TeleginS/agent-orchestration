# PR #198: Unit test coverage for ProgressStore

> Снимок от 2026-09-06. [Оригинал в GitHub — нужен доступ к TrafficRulesApp](https://github.com/TeleginS/TrafficRulesApp/pull/198).

Создан: 2026-09-01T21:37:53Z. Состояние на момент снимка: open. Merge: не выполнен.

## Описание PR

#### Summary

Adds unit test coverage for `ProgressStore` — the only store in the project without any. Implements Epic #193.

Closes #194, #195, #196, #197.

- **#194 / #195** — `TrafficTestAppTests/ProgressStoreTests.swift` (15 tests): the Solved set and its monotonicity, `markSolved` idempotence, accuracy rounding and the zero-submission case, `UserDefaults` round trips, `reset()` zeroing every field, plus the theme-level API (`closedThemesCount`, `isThemeClosed`, `themeProgress`). All denominators come from `QuestionBank`, never from `themes.json` or a literal; one test pins the set of themes-with-questions to exactly `0..<9` so a 10th theme fails loudly instead of silently under-counting in `ProgressStore.closedThemesCount`.
- **#196 / #197** — `TrafficTestAppTests/TestSessionProgressTests.swift` (17 tests): all five duration-accumulation sites (`completeTest`, `timeExpired`, both `exitTest` branches, `completePractice`) asserted as deltas; the #144 regression guard (exiting an already-completed session adds nothing) with real before/after snapshots; live capture in `submitAnswer()` including the ADR-0008 headline rule that a later wrong answer keeps a question Solved while returning it to Mistakes; `clearHistory()` resetting Progress alongside results and mistakes.

Both classes use a per-test ephemeral `UserDefaults(suiteName:)` injected through the stores' existing `init(defaults:)` points, dropped in `tearDown` via `removePersistentDomain(forName:)`. A suite-named `UserDefaults` does not fall through to the app's persistent domain — verified empirically, twice.

**No production code is modified.** The diff is two files under `TrafficTestAppTests/`.

#### Test plan

- [x] `xcodebuild build` — succeeds
- [x] Full suite, iPhone 17 — **157 passed / 0 failed / 0 skipped**
- [x] Full suite, iPhone 11 Pro Max — 157 / 0 / 0
- [x] Full suite with `-parallel-testing-enabled NO` — 157 / 0 / 0
- [x] Isolated run of the two new classes — 32 / 32
- [x] Verified hermetic: sibling classes leave `progress*` residue in `UserDefaults.standard` and run *before* these classes in the same host process, yet `testFreshStoreIsZero` still passes
- [x] Verified no suite plists leak to disk after a run

Counts taken from `xcrun xcresulttool get test-results summary`, not console grep — the parallel log writer can clobber a `Test case '...' passed` line and make `grep -c` undercount.

#### Out of scope

- **#192** — repeated `exitTest()` double-counts time and saves a duplicate `TestResult`. Deliberately untested here: the test would be red and would force a production change inside a tests-only task.
- **#202** — three sibling test classes leave `progress*` keys in `UserDefaults.standard`. Pre-existing, awaits its own task.
- QA findings on this PR's own first submission: #199, #200, #201 — all fixed in the commits after `ae659e9`, kept open until merge.

#### Also included: `.claude/project-profile.md`

Outside the test-coverage scope, included as a worked example of the pipeline's project profile (commit `9a26da8`).

The pipeline's six role prompts are deliberately stack-neutral — they describe how a reviewer reviews, not what a Swift reviewer checks. Everything project-specific lives in this one file: build and test commands with the `DEVELOPER_DIR` prefix this machine requires, the layout and its naming traps, architecture invariants including a *does not exist* list, release gates, gating rules, stack-specific review checklist entries and known risk areas. The orchestrator resolves it once and passes its path into every subagent launch.

Precedence is **observed code > profile > role prompt**: agents follow the code when the profile is wrong and report the drift. Three such reports came out of this run and are already folded in — the question count (2861, not the 2,863 `CLAUDE.md` still states), the test-file count and its baseline, and the fact that `grep`-ing `xcodebuild` stdout undercounts results because the parallel log writer clobbers a line. It also now records #192 and #202 as known risk areas.

It sits next to `.claude/agent-memory/`, which is version-controlled under the same convention.

#### Process note

This PR was produced by a multi-agent delivery pipeline (plan → implement → review → QA → fix loop). The full review and QA reports are posted as comments below: three review iterations and two QA passes. QA blocked the first submission on three findings that the review had approved; those are the commits after `ae659e9`.

## issuecomment-5500796064

Автор: TeleginS. Опубликован: 2026-09-01T21:39:25Z.

#### Code Review — Iteration 1

*Posted as a comment: all pipeline agents authenticate as the same user, and the platform blocks approving your own PR (`Can not approve your own pull request`). The orchestrator gates on the verdict string, not on the platform review state.*

**Scope reviewed:** `git diff master...HEAD` @ `ae659e9` — 2 files, +507, both under `TrafficTestAppTests/`. Zero production changes (confirmed by `--stat`).

##### 🔴 CRITICAL — none
##### 🟠 ARCHITECTURAL — none
##### 🟡 BUG — none

##### 🟢 QUALITY (should fix)

1. **`TestSessionProgressTests.swift`** (live-capture section, ~line 175)
   Problem: the ADR's most distinctive Solved rule is untested at the integration layer. `CONTEXT.md:225-227` and ADR-0008 both state that a later wrong answer does **not** un-solve a question — it only re-adds it to Mistakes. `testWrongAnswerGoesToMistakesNotToSolved` covers only wrong-on-a-never-solved question. `testSolvedSetOnlyGrows` covers monotonicity at the store layer, where it is true by construction (`markSolved` is additive; nothing but `reset()` removes). Nothing exercises solve → later wrong answer → still Solved *and* back in Mistakes.
   Fix required: one test mirroring `testSolvingTheSameQuestionAgainCountsForAccuracyOnly` with `correctly: false` on the second practice run — assert `solvedQuestionIds.contains(id)` still true, `mistakesStore.allIncorrectQuestionIds.contains(id)` true, `correctSubmissions` unchanged, `totalSubmissions == 2`.

2. **`ProgressStoreTests.swift:153-161`**
   Problem: `ProgressStore.closedThemesCount` hardcodes `(0..<9)` (`ProgressStore.swift:66`) and no test pins that literal to the data. `testThemeClosesWhenEveryQuestionIsSolved` asserts `closedThemesCount == 1`, which holds for any upper bound > 3. A 10th theme added to `questions.json` would make the store silently under-count with the suite still green — exactly the class of drift the plan's "don't hardcode counts" rule exists to prevent.
   Fix required: assert the themes-with-questions set is exactly `0..<9`, e.g. `XCTAssertEqual((0..<20).filter { bank.getQuestionCount(for: $0) > 0 }, Array(0..<9))`.

3. **`ProgressStoreTests.swift:202`** — `let otherThemeId = smallestThemeId == 0 ? 1 : 0` is a dead conditional; `smallestThemeId` is a `let` constant `3`. Write `0`.

4. **`TestSessionProgressTests.swift:265`** — `RunLoop.current.run(until:)` diverges from the house convention `RunLoop.main.run(until:)` (`TestSessionStateResetTests.swift:71`). Equivalent on XCTest's main thread, but `.current` is documented to return immediately when no input sources are attached. Cosmetic.

5. **Observation, not a diff defect.** `testTerminalPathOnUnstartedSessionAccumulatesNothing` exercises `completeTest()` on a never-started session. Besides the `?? 0` duration branch it means to pin, that path also writes a phantom `TestResult` into history (`TestSession.swift:368-376`: `totalQuestions: currentQuestionIndex + 1` = 1 against an empty `testQuestions`). Unreachable from the UI, same family as #192. Worth filing so it is documented rather than merely unasserted.

---

#### ✅ APPROVAL (conditional — items 1 and 2 are worth one short follow-up pass; nothing blocks merge)

##### What I verified

**Non-vacuity — the primary risk in this review.**
- Every terminal-path test starts a session with a config. The single unstarted case is the one #196 explicitly requires.
- The three "does not count twice" tests are load-bearing, not accidentally green: `completeTest()`, `timeExpired()` and `completePractice()` all route through `trackTestCompleted(passed:)` (`TestSession.swift:478-486`), which sets `naturalCompletionLogged = true`; the `exitTest()` guard at `:399` then short-circuits. Remove the guard and each would add a second strictly-positive duration, failing the equality assertion.
- `letSessionRun()` genuinely blocks: single-path tests run ~0.105 s, double-spin tests ~0.157 s. The "strictly > 0" assertions rest on real elapsed time, not sub-microsecond noise. No `sleep()`; whole class runs in ~1.9 s.
- All five accumulation sites are covered: `completeTest():366`, `timeExpired():162`, `completePractice():386`, `exitTest()` practice branch `:409`, `exitTest()` non-practice `:419`.

**Semantics match ADR-0008 / the CONTEXT glossary** — no test encodes "whatever the code does": Solved monotonic and idempotent; Closed = all questions Solved; an empty/unknown theme is *not* Closed (`!ids.isEmpty` guard); accuracy rounds (67 at 2/3) rather than truncates and returns 0 on zero submissions; denominators come from `QuestionBank`, never `themes.json`; practice counts toward time, submissions and Solved; `clearHistory()` is one full reset across all three stores.

**Isolation.** Both classes clear the four `progress*` keys in *both* `setUp` and `tearDown`. This is necessary, not ceremonial: sibling classes construct `TestSession` with the default `ProgressStore()` and clear only `testResults`/`incorrectQuestions`, so they do leak `progress*` into `UserDefaults.standard` — the new `setUp` clears absorb it.

**Acceptance criteria.** All of #194, #195, #196, #197 and the #193 Definition of Done are met. `180` / `2861` are derived from `bank`; only theme *ids* are literals, as the plan permits. The #192 double-`exitTest()` case is correctly absent.

**Conventions and build.** `@testable import TrafficTestApp`, `final class`, `// MARK:` sections, private helpers at the bottom. Glossary terminology throughout; no "answered" as coverage, "learned", or "pass rate". No profile anti-patterns. `project.pbxproj` correctly untouched — `objectVersion = 77` with `PBXFileSystemSynchronizedRootGroup`.

**Tests run.** Full suite → exit 0. Targeted: 14/14 `ProgressStoreTests` + 16/16 `TestSessionProgressTests`.

Not applicable to this diff: no user-facing strings, no gated capability, no `debugPremiumMode` call site, no persistence-key rename, no new API surface.

##### Profile drift

- The active profile says "Tests: `TrafficTestAppTests/` (13 test files)". Correct before this branch; now 15.
- ADR-0008 (Consequences) references `QuestionBank … questionCount`; the actual property is `totalQuestionsCount` (`QuestionBank.swift:113`). Doc drift only — the tests use the correct accessors.

## issuecomment-5500799535

Автор: TeleginS. Опубликован: 2026-09-01T21:39:44Z.

#### Code Review — Iteration 2

Re-review of the fix pass `ae659e9..e19273c` (+26 / −2).

#### ✅ APPROVAL

##### Finding 1 — coverage gap on the ADR headline rule: **RESOLVED, and the test is genuinely load-bearing**

`TestSessionProgressTests.swift:199-212`. I traced the new test against production rather than trusting its shape:

- **The question is genuinely Solved before the wrong answer.** Run 1's correct submission goes through `TestSession.submitAnswer` → `progressStore.markSolved(currentQuestion.id)`. `markSolved` is the monotonic guard the ADR is about, so `XCTAssertTrue(solvedQuestionIds.contains(id))` is exactly the assertion that fails if that monotonicity is ever broken.
- **The second run really submits an incorrect answer.** The trap was `selectAnswer`'s `guard selectedAnswers[currentQuestionIndex] == nil`, which would silently swallow the second answer if state leaked between runs. It does not: `session.start` calls `initQuestionStatuses()`, which clears `selectedAnswers`.
- **The assertions cannot pass vacuously.**
  - `correctSubmissions == 1` proves run 1's answer was actually correct — so "Solved" is not in the set for an unrelated reason.
  - `correctSubmissions == 1` (not 2) *also* proves run 2's answer was actually wrong. The helper builds the wrong answer as `(correctAnswer + 1) % options.count`; a collision with the correct index would make it 2 and fail the test. It cannot collide — theme 0's first question has 3 answers, and `getQuestionIds(for: 0)[0]` is deterministic since `questionsByTheme` is built in JSON order.
  - `mistakesStore.allIncorrectQuestionIds.contains(id)` is not pre-satisfied: storage is cleared in `setUp`, and run 1's correct answer calls `markCorrect` on an already-empty set. The id is in Mistakes only because of run 2.
  - `totalSubmissions == 2` pins that both submissions were counted.

Mutating any of the three production behaviours breaks this test. It is not a coverage fig leaf.

##### Finding 2 — hardcoded `(0..<9)`: **RESOLVED**

`ProgressStoreTests.swift:148-152` uses a real `QuestionBank()`, which loads bundled `questions.json` via `Bundle.main` — not a fixture. Verified independently: exactly themes `{0…8}`, counts `221/292/274/180/331/241/453/593/276`, 2861 total. The assertion also catches a bundle-load failure, since an empty bank yields `[] != Array(0..<9)`.

##### Finding 3 — dead conditional: **RESOLVED**
##### Finding 4 — `RunLoop.main`: **RESOLVED**

Same object as `RunLoop.current` on the main thread today; the change removes the trap rather than fixing a live bug.

##### Verified independently

- Full suite with the profile's exact command: **157 passed / 0 failed**. Static count of `func test` declarations also gives 157.
- **No production code touched.** `git diff --stat master...HEAD` is two test files, nothing else — no `.pbxproj`, no source.
- **No new isolation, ordering or flakiness problem.** `testExactlyNineThemesHaveQuestions` writes no shared state and is order-independent. The new `TestSession` test writes only keys already in the class's clear list, calls no `letSessionRun()` and asserts no wall-clock value, so it adds zero timing dependence. Runtimes 0.063 s and 0.054 s.

##### 🟢 Quality observations (non-blocking, no fix requested)

1. `testExactlyNineThemesHaveQuestions` pins the **data** to 9 themes, not the `(0..<9)` **literal** in `ProgressStore.closedThemesCount`. Narrowing that literal to `(0..<8)` would still pass, because every closed-theme test uses theme 3. A "close theme 8, expect `closedThemesCount == 1`" assertion would pin the upper bound too. The original finding is addressed; this is the residual half.
2. The `0..<20` scan window is itself an arbitrary bound; a theme id of 25 would slip past it. Trivial.
3. Forward-looking only: if a test here is ever declared `async`, XCTest may run it off the main thread, and `RunLoop.main.run(until:)` from a background thread is not valid. No async tests exist in this class today.

## issuecomment-5500803440

Автор: TeleginS. Опубликован: 2026-09-01T21:40:06Z.

#### QA — Pass 1

**Verdict: 🛑 BLOCKED** on three in-scope findings.

##### Test suite

Four runs, two destinations. Counts from `xcrun xcresulttool get test-results summary`, not console grep.

| # | Destination | Mode | Result |
|---|---|---|---|
| 1 | iPhone 17 | default (parallel) | 157 passed / 0 failed / 0 skipped |
| 2 | iPhone 11 Pro Max | default (parallel) | 157 / 0 / 0 |
| 3 | iPhone 17 | `-parallel-testing-enabled NO` | 157 / 0 / 0 |
| 4 | iPhone 17 | `-only-testing` the two new classes | 32 / 0 / 0 |

No skips, no flakiness across two devices and two execution modes. Class execution order differed between runs 1 and 3 with identical results, so the new classes are order-independent.

##### In-scope bugs

- **#199 (medium)** — five single-path tests assert `totalTestSeconds > 0` instead of a delta. Whenever the store is non-zero on entry the assertion is a tautology and cannot fail even if the accumulation line were deleted from the method under test. This is the half of PR #143 invariant 1 the task exists to pin; the "not twice" half was written correctly with real snapshots, so the file is internally inconsistent. Not theoretical — it already happened in this task, and the suite still leaves `progress*` residue behind (#202).
- **#200 (low)** — non-hermetic `UserDefaults.standard` with a hand-maintained key list. `removeObject` on `.standard` cannot clear a value arriving from another domain, which is exactly what bit this task. Full isolation is available without touching production code: all three stores already accept `init(defaults:)`.
- **#201 (low)** — the two `exitTest()` tests do not assert the property distinguishing the branches (practice saves no `TestResult`, normal saves one). If `start(_:)` ever set `isPracticeMode` wrongly, both would stay green while the claimed five-site coverage became false.

##### Pre-existing

- **#202 (low)** — `TestSessionStateResetTests`, `TestSessionAnalyticsTests` and `TestSessionRestartTests` leave `progress*` keys in `UserDefaults.standard`; their `clearUserDefaults()` was never updated after PR #143. Does not block this PR.

Referenced, not re-filed: **#192** (repeated `exitTest()` double-count) — mechanism confirmed at `TestSession.swift:399`, deliberately untested here.

##### Acceptance criteria

- **#194** — met, all 9. Every assertion is non-vacuous.
- **#195** — met, all 8. Denominators from `QuestionBank`, never `themes.json`.
- **#196** — **one criterion only nominally met.** The five single-path tests exist but assert the absolute total rather than the increase the criterion words (#199), and do not distinguish the two `exitTest()` branches (#201). The other 8 criteria are genuinely met — the four "not twice" tests use real snapshots and are the strongest tests in the file.
- **#197** — met, all 7.

Epic DoD: every public member of `ProgressStore` covered; `markSolved` idempotence and `reset()` zeroing solidly pinned; diff stays inside `TrafficTestAppTests/`; glossary terminology respected. "New tests leave no residue" is met — the only residue after a serial run traces to `TestSessionStateResetTests`, not to the new classes. Invariant 1 is only **half** pinned.

##### Profile drift

None in what I exercised. One **gap** worth adding (the profile is silent, so not drift): the shared scheme sets `parallelizable = "YES"`, so `xcodebuild test` runs on an ephemeral clone rather than the named device. Two consequences — class execution order varies between runs, and inspecting simulator `UserDefaults` after a run requires `-parallel-testing-enabled NO` or the clone is already gone.

---

To be clear about what is *not* wrong: the suite is genuinely green, the production code is untouched and behaving per ADR-0008, and the hardest part — the four "exiting an already-finished session counts nothing twice" tests, the actual #144 regression guard — is done correctly. The block is that the mirror half of the same invariant is written in a form that cannot fail under precisely the contamination that already occurred once during this task.

## issuecomment-5500807469

Автор: TeleginS. Опубликован: 2026-09-01T21:40:29Z.

#### Code Review — Iteration 3 (review gate on the QA fixes)

Pipeline rule 11: every code change made after QA begins must pass review before QA re-tests. Scope: `e19273c..114cb54` — 2 files, +49/−57, test code only.

#### ✅ APPROVAL

##### The test-count question — resolved, no coverage regression

**157 tests, 157 passed, 0 failed, 0 skipped.** Authoritative source: `xcrun xcresulttool get test-results summary` at `114cb54`.

The developer's reported "156" is a **log-parsing artifact**, not a lost test. Under parallel execution one line is clobbered mid-write:

```
Test case 'PaywallAnalyticsTests.testAnalyticsSessionSuppressesDismissAfte2026-09-01 23:14:44.771 xcodebuild[70252:4510744] [MT] IDETestOperationsObserverDebug: ...
```

so `grep -c "' passed"` returns 156. Corroborating evidence:
- Per-file `func test` counts are **identical** at `e19273c` and `114cb54` across all 15 files (157 total).
- No `XCTSkip` anywhere; no test method with a non-conforming signature.
- Diffing executed-test names against source-declared names yields exactly that one truncated line and nothing else.

Process note: do not derive the QA-gate count from `xcodebuild` stdout. Use `xcrun xcresulttool get test-results summary --path <bundle>.xcresult`.

##### #200 (hermetic suites) — verified correct, including the fall-through question

Whether `UserDefaults(suiteName:)` reads fall through to the application domain is the right thing to ask, and I checked it two ways rather than trusting doc-memory:

1. **Isolated probe** — wrote a key into `UserDefaults.standard`, `synchronize()`d, then read it through a freshly created suite instance: `nil`. `dictionaryRepresentation()` on the suite has exactly one fewer key than standard's, and the missing one is the probe key.
2. **In situ, which is stronger** — the simulator's app domain is polluted right now (`progressSolvedQuestionIds`, `progressCorrectSubmissions`, `progressTotalSubmissions`). Despite that, `testFreshStoreIsZero` and — decisively — `testResetClearsStorage`'s `XCTAssertNil(defaults.array(forKey: "progressSolvedQuestionIds"))` both pass. If the suite fell through, both would fail.

**Injection is complete, not partial.** Every store construction in both classes routes through an injected `defaults`, including both persistence round-trip `reloaded` stores (`ProgressStoreTests.swift:108`, `:142`; `TestSessionProgressTests.swift:238`). All three stores route every read and write through the injected instance — no internal `.standard` escape hatch. Zero remaining `UserDefaults.standard` references in either file.

**Suite name is per-test, not per-class** — built in `setUp` from a fresh UUID, so parallel or reordered execution cannot collide. `removePersistentDomain(forName:)` does clean up: after two full runs, `find` returns zero leftover plists.

##### #201 (exitTest branches) — matches the implementation

Checked against `TestSession.exitTest()` (`:396-430`): the practice branch returns at `:410` before reaching `resultsStore.add(result)`; the normal branch adds exactly one at `:429`. Both new assertions are load-bearing — moving `add` above the `if isPracticeMode` block fails the practice test; deleting it fails the other.

##### Nothing weakened, nothing left behind

`XCTAssert` counts: `ProgressStoreTests` 40 → 40, `TestSessionProgressTests` 38 → 40 (+2 = precisely the two #201 additions). The diff contains no other assertion edits — only the five `> 0` → `> before` rewrites and the `UserDefaults.standard` → `defaults` substitutions. No mutation-test scaffolding survived (`totalTestSeconds = 500`, seeding helpers, TODO/FIXME markers: none).

##### 🟢 QUALITY (non-blocking)

1. **The #199 delta form is correct, but credit it accurately.** With the hermetic suite from #200, `before` is `0` in all five tests — always — so `> before` is numerically identical to the old `> 0`. The developer's mutation proof showed the delta form catches a regression only when the store is dirty on entry, and the #200 fix makes that state unreachable. The two fixes are individually right, but the proof does not transfer to the committed suite: the delta form is regression-resistance against a future `setUp` that seeds state, not a defect the old form was missing today. Still the form I want, and it does catch a terminal path that accumulates nothing (`0 > 0` fails). No change requested.
2. **The refactor removed an incidental janitor.** `ProgressStoreTests.clearUserDefaults()` used to scrub the four `progress*` keys from the shared app domain. Sibling classes build `TestSession(analytics:)` with default, non-injected stores and none of their clear lists include `progress*` — that residue is the pollution found on disk above. Zero impact today; filed as #202.

##### Profile drift

The active profile says "Tests: `TrafficTestAppTests/` (13 test files)" — there are now 15, totalling 157 tests. Worth recording the count as a stated baseline so the next iteration can spot a real regression instead of chasing a grep artifact.

**Verdict: approved at iteration 3 — hand to QA for re-test.**

## issuecomment-5500811713

Автор: TeleginS. Опубликован: 2026-09-01T21:40:53Z.

#### QA — Pass 2 (re-test)

**Verdict: ✅ GREEN.**

##### Test suite

Four full runs, two destinations, all green. Counts from `xcrun xcresulttool get test-results summary`.

| Run | Destination | Condition | Passed | Failed | Skipped |
|---|---|---|---|---|---|
| 1 | iPhone 17 (iOS 26.5) | baseline | 157 | 0 | 0 |
| 2 | iPhone 17 | `.standard` deliberately poisoned | 157 | 0 | 0 |
| 3 | iPhone 17 | baseline | 157 | 0 | 0 |
| 4 | iPhone 11 Pro Max (iOS 26.5) | baseline | 157 | 0 | 0 |

**157 confirmed.**

##### My three findings

**#199 — RESOLVED.** All five tests snapshot `before` after `letSessionRun()` and immediately before the terminal call, so the delta measures exactly that path's contribution.

On the reviewer's caveat that `before == 0` today: agreed on the arithmetic, but the finding was never about today's number. `> 0` asserted a *state fact* about the store rather than the *invariant* the test names. Under the old shared `.standard`, `> 0` could pass with the terminal path contributing nothing, because a sibling class had already made the store non-zero. #199 and #200 were one defect seen from two sides; fixing #200 is what turns `> before` into a real check. Resolved on the merits, not just on shape.

**#200 — RESOLVED**, verified empirically on three axes:

- *Foundation semantics probe.* With `progress*` keys written into the standard domain, a fresh `UserDefaults(suiteName:)` returned `nil`/`0`/`0.0` for all of them — **no fallthrough to the app's persistent domain**. This was the main worry, since suite-named defaults are widely believed to inherit the app domain in their search list. They do not.
- *In-process proof (strongest, no filesystem inference).* In all four runs `TestSessionStateResetTests` and `TestSessionAnalyticsTests` run **before** `ProgressStoreTests` **in the same test-host process**. Both build `TestSession(analytics:)` → default `ProgressStore()` on `.standard`, and neither scrubs `progress*`. So `.standard` provably holds non-zero progress values by the time `ProgressStoreTests` runs — and `testFreshStoreIsZero` still asserts all four fields are zero, passing every time.
- *Nothing leaks to disk.* Zero files matching the suite-name patterns anywhere under `~/Library/Developer/CoreSimulator/Devices` after every run.

The poisoned run (2) corroborates but I am **not** treating it as decisive — `cfprefsd` buffers writes and I could not prove from the filesystem that the poison was live at test time. The in-process evidence has no such weakness.

Note: class execution order is **not alphabetical and not stable** (`ProgressStoreTests` moved between positions 5 and 6 across runs). The hermetic suite is exactly what makes these classes order-independent.

**#201 — RESOLVED and genuinely discriminating.** Against `TestSession.swift:396-430`: the practice branch (405–411) accumulates and returns without saving; the normal branch (413–429) accumulates and calls `resultsStore.add` once. Swapping the branches would now fail one of the two tests — which is what was missing.

##### New bugs

**In-scope: none.**

**Pre-existing (non-blocking, architecture note, not filed):** `QuestionBank.swift:15` does `private var languageSettings = LanguageSettingsManager.shared`, which reads `UserDefaults.standard`. So `QuestionBank()` retains a `.standard` dependency even inside the now-hermetic tests. No observable impact — no assertion in either new file depends on resolved language — but it violates the profile's own "dependencies injected, not reached for via `.shared`" checklist item.

I checked and **rejected** two candidates rather than file them speculatively: `isPremiumProvider` defaulting to `SubscriptionManager.shared` (reached only from `restartTest`'s `.theme` branch, which these tests never call), and the force-unwraps in `tearDown` (can only fail for a bundle-id or `NSGlobalDomain` suite name; a UUID cannot).

**Regressions: none.** Per-file `func test` counts identical across all three commits (15 + 17 = 32). The fix pass only added assertions (2) and strengthened existing ones — none weakened or removed. Three assertions became meaningfully stronger as a side effect of hermeticity (`testMarkSolvedIsIdempotent`, `testResetClearsStorage`, and the `XCTAssertFalse(resultsStore.testResults.isEmpty)` pre-condition), since each could previously have turned on `.standard` residue. No `sleep()`; `RunLoop.main.run(until:)` only.

##### Acceptance criteria — #196 now genuinely met

- The criterion I marked *nominally* met last pass was satisfied in letter by `> 0` but not in substance. With #199 and #200 fixed it is now met in substance.
- **One criterion is now stale by design.** It mandated that `setUp`/`tearDown` clear `testResults`, `incorrectQuestions` and the four `progress*` keys. The fix pass replaced key-scrubbing with a per-test ephemeral suite — strictly stronger, since it isolates writes too and removes the hand-maintained key list that *was* the root of #200. Intent met more completely than the letter.

#194, #195, #197 remain met.

##### Profile drift

- Test-file count 13 → 15 on this branch.
- The Commands section had no authoritative test-count method. Console `grep -c` is unreliable under the parallel log writer.

Both have been corrected in the active profile.
