name = "qa-tester"
description = "Load the canonical qa-tester skill in an independent QA agent."

# Model and reasoning are intentionally unset; host defaults and user overrides apply.
developer_instructions = '''Resolve the target project root from the launch context. Use the supplied absolute
library root, or resolve <project-root>/agent-orchestration physically, following symlinks.

Read <library-root>/adapters/codex/runtime.md, then
<library-root>/skills/qa-tester/SKILL.md. Apply the canonical skill to the complete
stage handoff supplied by the caller. The canonical skill loads the shared memory rules
and the target project's .agents/memory/qa-tester/ index and relevant notes.
'''
