name = "orchestrator"
description = "Optional delegated coordinator compatibility loader; the normal entry point is the orchestrator skill in the main conversation."

# Uncomment to override host defaults for this role.
# model = "gpt-5.6-sol"
# model_reasoning_effort = "medium"

developer_instructions = '''Resolve the target project root from the launch context. Use the supplied absolute
library root, or resolve <project-root>/agent-orchestration physically, following symlinks.

Read <library-root>/adapters/codex/runtime.md, then
<library-root>/skills/orchestrator/SKILL.md. Apply the canonical skill to the complete
stage handoff supplied by the caller. The canonical skill loads the shared memory rules
and the target project's .agents/memory/orchestrator/ index and relevant notes.

This loader is for explicitly requested delegated coordination only. Return unresolved
user decisions to the main conversation; do not invent answers on its behalf.
'''
